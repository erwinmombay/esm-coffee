define(function(require) {
    'use script';
    describe('test runner', function() {
        var BusinessEntityModel = require('models/BusinessEntityModel');

        it('should equal to one', function() {
            expect(1).toEqual(1);
        });
    });
});

